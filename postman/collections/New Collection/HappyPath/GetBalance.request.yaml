$kind: http-request
name: GetBalance
url: "{{payments_url}}/accounts/balance"
method: GET
headers:
  X-User-Id: "{{user_id}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"статус 200\", () => {\r

      \    pm.response.to.have.status(200);\r

      });\r

      \r

      setTimeout(() => {}, 2000);"
    language: text/javascript
order: 2946639903124446

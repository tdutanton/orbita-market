$kind: http-request
url: "{{payments_url}}/accounts"
method: POST
headers:
  X-User-Id: "{{user_id}}"
  Connection: close
scripts:
  - type: afterResponse
    code: "pm.test(\"статус 201\", () => {\r

      \    pm.response.to.have.status(201);\r

      });"
    language: text/javascript
  - type: beforeRequest
    code: "const userId = crypto.randomUUID();\r

      pm.environment.set(\"user_id\", userId);"
    language: text/javascript
order: 4074858022437053

$kind: http-request
name: GetBalance
url: "{{payments_url}}/accounts/balance"
method: GET
headers:
  X-User-Id: "{{user_id}}"
  Connection: close
scripts:
  - type: afterResponse
    code: "pm.test(\"статус 200\", () => {\r

      \    pm.response.to.have.status(200);\r

      });\r

      \r

      const response = pm.response.json();\r

      \r

      pm.test(\"New Balance is 400\", function () {\r

      \    pm.expect(response.balance).to.eql(400.0);\r

      });"
    language: text/javascript
order: 4385846782907854

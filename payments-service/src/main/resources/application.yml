server:
  port: ${SERVER_PORT:8080}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: always

validate:
  min-deposit-amount: 0.0

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:payments-db}:5432/payments_db?sslmode=disable&connectTimeout=10&socketTimeout=30
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    consumer:
      group-id: ${KAFKA_GROUP_ID:payments-service}
    topics:
      payment-requested: ${PAYMENT_REQUESTED_TOPIC:order.payment.requested}
      payment-completed: ${PAYMENT_COMPLETED_TOPIC:order.payment.completed}
      payment-failed: ${PAYMENT_FAILED_TOPIC:order.payment.failed}
